/** Production environment (mobile-app change): native builds talk to the VPS. */
export const environment = {
  production: true,
  apiBaseUrl: '',
  nativeApiBaseUrl: 'https://cercetador.albascout.ro',
};
